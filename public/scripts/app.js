// Client facing scripts here
// Character limit for 'tweets' and targets
// const $menuItem       = $("");
const $window         = $(window);
const $main           = $('main');
const $page           = $('html');

$(document).ready(() => {
  loadMenu();
});
