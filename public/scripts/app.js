// Client facing scripts here
// Character limit for 'tweets' and targets
const $menuItem      = $("");
const $window         = $(window);
const $mainContainer  = $('main');
const $page           = $('html');

$(document).ready(() => {
  loadMenu();
});
